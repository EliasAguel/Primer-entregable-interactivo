let precioPersona = (cantidadPersonas, precio) => cantidad * precio;
let iva = (precioPersona) => precioPersona * 0.21;
let precioFinal = (iva, otrosServicios) => iva = otrosServicios;

if (cantidadPersonas = cantidadDisponible) {
    alert (`El precio final es ${precioFinal}`);
}
else {
    alert (`La cantidad de entradas solicitadas no estan disponibles`)
}

